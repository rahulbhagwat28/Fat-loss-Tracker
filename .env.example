# Get a Postgres URL from Neon (https://neon.tech), Vercel Postgres, or any Postgres host.
# For local SQLite development you can use: file:./dev.db with provider = "sqlite" in schema.prisma (revert that change).
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
