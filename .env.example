# Required for Vercel and for local dev. Get from: https://neon.tech → New project → copy connection string.
# On Vercel: Settings → Environment Variables → add DATABASE_URL with this value.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
